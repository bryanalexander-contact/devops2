name: CI/CD Innovatech Chile

on:
  push:
    branches:
      - deploy

jobs:
  build-and-publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      # Loguearse en Docker Hub para cumplir la rúbrica de publicación
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      # Construir y publicar el Frontend usando su raíz real (donde está su dockerfile)
      - name: Build and Push Frontend
        run: |
          docker build -t ${{ secrets.DOCKERHUB_USERNAME }}/innovatech-frontend:latest ./frontend_devops
          docker push ${{ secrets.DOCKERHUB_USERNAME }}/innovatech-frontend:latest

      # Construir y publicar el Backend 1 (Despachos)
      - name: Build and Push Backend 1
        run: |
          docker build -t ${{ secrets.DOCKERHUB_USERNAME }}/innovatech-backend1:latest ./back-Despachos_SpringBoot/Springboot-API-REST-DESPACHO
          docker push ${{ secrets.DOCKERHUB_USERNAME }}/innovatech-backend1:latest

      # Construir y publicar el Backend 2 (Ventas)
      - name: Build and Push Backend 2
        run: |
          docker build -t ${{ secrets.DOCKERHUB_USERNAME }}/innovatech-backend2:latest ./back-Ventas_SpringBoot/Springboot-API-REST
          docker push ${{ secrets.DOCKERHUB_USERNAME }}/innovatech-backend2:latest

  deploy-to-ec2:
    needs: build-and-publish
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      # Copiar solo el docker-compose.yml al servidor de Amazon Linux
      - name: Copy docker-compose via SSH
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ec2-user
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          source: "docker-compose.yml"
          target: "/home/ec2-user"

      # Conectarse a la EC2, descargar las imágenes nuevas de Docker Hub y encender todo
      - name: Executing remote ssh commands
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ec2-user
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd /home/ec2-user
            # Modificar el docker-compose en caliente mapeando las rutas exactas
            sed -i 's|build: ./frontend_devops|image: '${{ secrets.DOCKERHUB_USERNAME }}'/innovatech-frontend:latest|g' docker-compose.yml
            sed -i 's|build: ./back-Despachos_SpringBoot/Springboot-API-REST-DESPACHO|image: '${{ secrets.DOCKERHUB_USERNAME }}'/innovatech-backend1:latest|g' docker-compose.yml
            sed -i 's|build: ./back-Ventas_SpringBoot/Springboot-API-REST|image: '${{ secrets.DOCKERHUB_USERNAME }}'/innovatech-backend2:latest|g' docker-compose.yml
            
            # En Amazon Linux se ejecuta con guion intermedio 'docker-compose'
            /usr/local/bin/docker-compose down || true
            /usr/local/bin/docker-compose pull
            /usr/local/bin/docker-compose up -d